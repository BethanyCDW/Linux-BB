# You need pip, a python packet manager, to install Ansible on Amazon Linux 2023 and newer. Neither amazon-linux-extras repository nor EPEL is available on Amazon Linux 2023.
# Update system package
sudo dnf update
# Install pip
sudo dnf install python3-pip
# Type y and hit enter to continue install
# Verify install
pip --version
# Install Ansible
pip install ansible
# Verify Ansible install
ansible --version
# Uninstall Ansible
pip uninstall ansible
# Type y and hit enter to continue uninstall
